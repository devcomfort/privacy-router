모든 개발이 다 끝난 후에 어댑터 패턴으로 에이전트에 쉽게 통합할 수 있게 구성해야해.

===

MCP, OpenResponse API, OpenAI Compatible API 호환성을 확보하기 위해서 중간 계층이 되는 데이터 스키마를 만들고 원할 때 각 표준에 호환되는 형태로 데이터를 변경할 수 있으면 좋겠어.
pydantic 기반의 스키마 작성이 가능할까?









===

우리가 core/ 패키지를 따로 둔다면 디렉토리 구조를 어떻게 디자인 할 수 있을까?

모든 import 구문이 코드 파일 상단에 위치하도록 정렬해야해. 정렬하고 테스트가 신뢰 가능하게 구현될 때까지 계속 작업해줘.

데이터베이스에 API키도 저장할 수 있도록 디자인하고 싶은데, 최종적으로 데이터베이스 스키마와 백엔드 API 엔드포인트와 그 스키마를 어떻게 구성해야할까?
보안상 평문으로 저장하면 안 된다는 점을 고려하여 답변해줘.

i18n의 처리가 아직 완벽하지 않은 것 같아.                                       |
매 라운드마다 agent-browser로 모든 페이지 또는 코드를 점검하면서 전수 조사 결과에서 문제가 발견되지 않을 때까지 문서를 수정해줘.

4B 이하의 모델을 extractor, judge 등에 사용하는 것을 상정하고
EXAONE 4.0 1.2b, Ministral 3 3b, Gemma4 e4b, Gemma4 e2b 모델을 대상으로 파라미터 및 프롬프트를 튜닝해줘.
llama-server 및 vllm, sglang을 타겟으로 테스트를 하면 되고.
필요하면 docker를 기반으로 얘네들 설정 먼저 해.

Dockerfile, docker-compose 뭐든 필요한대로 작업하면돼. 더 높은 개발자 경험 및 유지보수성을 만들 수 있는 방향이 좋은 방향이라고 생각해.

각 엔진별 특징이나 제약이 있을거야. response_format 같은 파라미터를 작성하거나 또는 특정 플래그를 지정해줘야 모델을 돌릴 수 있는 경우도 있을거고, 최신 모델의 경우는 특정 엔진은 지원하지만 또다른 엔진은 지원하지 않거나, 코드가 이미 구현되어 있지만 미완성 형태라서 접근하기 어려운 형태로 구현된 경우(vLLM의 EXAONE 4.5 아키텍처 불완전 지원)가 있을 수 있어.
가능하다면 몽키패치로 어떻게든 모델을 사용해보는 방향으로 진행하면 돼.

튜닝 시 점수 측정은 N>=5 횟수로 독립 시행하여 통계적 유의성을 확보해야해.
그리고 테스트 데이터셋에 대해서 필요하다면 피룡한만큼 보완해도 돼.
어떤 종류의 테스트가 필요한지, 어떤 표면에서 검증이 되어야하는지 등을 고찰하고 해당 부분을 검증할 수 있는 수준의 테스트케이스를 기획해야해.

목표 세우고, 시행 순서 세우고 차근차근 진행해봐.
느려도 되니까 신뢰성이 최우선사항이야.