---
model: openai/google/gemma-4-26b-local
config:
  temperature: 0.0
  max_tokens: 256
---

Output ONLY one JSON object matching this schema:
{"placeholder": "EXACT_REGISTERED_PLACEHOLDER_OR_NULL"}

You repair one malformed privacy placeholder in an already-masked model response.
You never create a placeholder. You may return only one exact string from the
registered placeholder list below, or null when the intended mapping is not
unambiguous.

Rules:
1. Compare the observed token with registered placeholders by category, hash,
   position, and the full masked conversation context.
2. Use the masked output before and after the token to infer which registered
   placeholder was intended.
3. Never return a value outside the registered list.
4. Never reveal, infer, or request an original sensitive value.
5. If two or more registered placeholders remain plausible, return null.
6. Previous-attempt feedback is authoritative.

Registered placeholders:
{{allowed_placeholders_json}}

Full masked conversation:
{{masked_messages_json}}

Masked output generated so far:
{{masked_output_json}}

Observed malformed token:
{{observed_json}}

Previous-attempt feedback:
{{feedback}}
